import stdio
import sys
from markov_model import MarkovModel


def replaced_unknown(self, corrupted):
    if corrupted[i] = '~':
        k = self.order()
        kgram = corrupted[i - k: i]
        keys = self._st[kgram].keys()
        vals = [self._st[kgram][t] for t in keys]
        original += keys[argmax(vals)]


def main():
    return none


if __name__ == '__main__':
    main()
